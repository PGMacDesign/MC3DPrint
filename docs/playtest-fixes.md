# Playtest Fixes — post-launch QA round (2026-06-16)

Running list of issues found during live testing. **Process:** fix one at a time → check it →
check it off here → commit + push (build files + this doc) → next. The jar is **NOT** redeployed
during active testing (user has the game open) — redeploy once the user signals they're done.
Leftover unchecked items carry over to the next session.

## Done
- [x] jungle_hut — grounded (floor y0, door sill y1), no stilt float; **grass_block made printable** (was unprintable/allowlisted, floated the footing)
- [x] cherry_blossom_pavilion — added a walk-in entrance, then a matching door on the opposite side (symmetry)
- [x] badlands_mesa_dwelling — dropped filler foundation (door sill y1), moved entrance to the open face
- [x] hot_air_balloon — removed the 2 purposeless plank base courses (basket grounded)
- [x] hobbit_hole — revamped: grounded, round walk-in door, grass berm, bed no longer in the wall
- [x] treehouse — hollowed the central trunk for interior room + moved ladder clear of the front door
- [x] snowy_igloo — closed the dome roof gap + tunnel entrance + standing headroom
- [x] desert_pyramid_shrine — grander 4-tier pyramid, facade motif, treasure chamber
- [x] taiga_spruce_longhouse / snowy_alpine_chalet / nordic_viking_longhouse — dropped redundant filler foundation course (door reachable)
- [x] jungle_temple_ruin — fixed door/glass upper-half overlap
- [x] underwater_dome_base — added a walk-in iron door, removed the unnecessary surrounding water
- [x] (infra) signText() helper + printer syncs block-entity data on print; deterministic BE serialization; double-block + foundation guardrails
- [x] storage_barrel_hall — removed entrance + center hanging lamps, tucked the other 3 into the corner nooks (walkways clear)

## Needs rework (farms)
- [x] iron_farm — **lava-cauldron kill** (water-safe, no obsidian, contained) + **2×2 drop** for 1.4-wide golems; hopper ring → chest. (lava_cauldron confirmed printable = itemless/structural.)
- [x] chicken_coop_auto — redesigned with contained **lava cauldrons** (no raw lava → no leak/obsidian); glass pen on a hopper grid, toss-in gap, accessible front chest, explanatory signs.
- [x] mob_xp_tower — clarity rework: walk-in kill chamber (door), AFK spot + iron-bar kill slot, glass viewing window, accessible chest, dry landing (removed base water), explanatory signs.

## Open (new findings, in order)
- [x] pumpkin_melon_farm — rearchitected for sideways growth: stems on hydrated farmland → fruit grows on adjacent dirt growth blocks (only valid spawn cell) → flowing-water sweep → hopper → chest (semi-auto; full-auto piston-crush can't fit one lane). Observers kept as growth indicators.
- [x] cactus_farm — rebuilt: spaced sand grow-blocks with air/water on all sides (so cactus survives), cobblestone breaker at the grow-into height, water sweep → hopper → chest, chests unblocked above. Cactus itself isn't FU-valued/structural so it can't print (like bamboo/kelp) — the player plants into the working mechanism (a sign explains).
- [x] bamboo_farm — rebuilt mirroring the fixed sugarcane farm: observer raised to the growth cell (re-fires each growth, not just once on plant) → piston break → water catch-column → hopper → chest; player plants bamboo on the soil cells (bamboo can't print, like cactus/kelp). A sign explains.
- [x] kelp_farm — redstone now rides a dedicated DRY stone shelf one column out (no longer on top of the observer/piston, no longer touching the water column → no wash-off on print); observer raised to the growth cell (y=2, 2nd kelp segment) so it re-fires each growth and breaks low (base regrows); piston snaps the cut stalk into the central water canal → hopper → chest; player plants kelp on the submerged stone grow-blocks (kelp can't print). Signs explain.
- [x] animal_pen — now prints a full grass_block floor at y=0 (grass_block became FU-valued this session, so the old "no floor/player terrain" rationale is obsolete); trough water + shelter plank floor preserved via overwrite ordering. **Spawn-egg decision RESOLVED** (Patrick chose "empty chest only"): added an unstocked feed/storage chest in the NW shelter corner (1,1,1) — no creative-only spawn eggs handed out; you bring your own.
- [x] tree_farm — tore out the fake observer/piston/dispenser auto-harvester (+ pointless hopper-under-dirt) and rebuilt as an HONEST semi-auto plantation (7×7×3): grass floor, oak-fence ring + gate, 6 oak saplings on true 2-block spacing with OPEN AIR above (trees grow into open world), chest+2-hopper collection sump by the gate, corner lanterns for light, sign explaining "plant spaced / chop & REPLANT / store". No fake auto, nothing caps the saplings.

- [x] apothecary_shop — removed the floating head-height herb row (potted_* + hanging lantern at y=2 across z=4 that blocked the aisle); relocated the 5 bloom types onto surfaces (bookshelf tops, barrel tops, counter); central aisle (x=2..4) now clear at y=1 and y=2 front-to-back; the one hanging lantern moved off the aisle against the west wall.

- [x] greenhouse — central aisle water channel (x=4, y=0) that the door dumped you into is now covered with flush waterlogged TOP slabs (walk above the water, dry feet, flush with the floor — no entry pit, walk in/out freely); each planter bed got its own y=1 water source so crops stay irrigated. No stairs/2nd level needed (single-storey).
- [x] tavern_inn — rebuilt the interior stair: moved it one bay east of the chimney (x=2), flipped it to `facing=south` so walking north actually ascends (it was facing=north = a riser-wall you'd jump/headbonk — the root cause), and extended it to a 6-step run (y=1..6) so the top step sits flush with the y=6 deck (no 1-block hop). Hatch re-derived (open at (2,6,6)/(2,6,7)) for full headroom; clean landing into the north room. Furnished both guest rooms (carpet rugs, barrel/chest nightstands w/ potted blooms, extra lanterns, bookshelves); both beds intact. Verified climb layer-by-layer in the dump.
- [x] stable_horse (Horse Stable) — the old north-wall ladder dead-ended into the solid loft slab at the eave (roof at (2,5,1) gave zero climb headroom there → couldn't ascend without breaking blocks). Replaced with a 4-step `facing=south` stair run in the front-half west stall climbing north ((2,1,8)→(2,2,7)→(2,3,6)→(2,4,5)) flush onto the loft's south edge, landing on the standable loft cell (2,4,4) (z≥3, roof y=8 = 3 blocks headroom). Reachable from the south bay / the aisle gate at (3,1,8); verified the full climb layer-by-layer in the dump. (Note: became a stair, not a ladder — the access works; tell me if you want the ladder aesthetic back.)

## Strategy note (farms)
Regrowing crops (cactus/bamboo/kelp/cane✓/melon✓) → make them actually work. Non-regrowing (trees) → simplify to honest semi-auto. Remaining unflagged farms to proactively check: villager_trading_hall, fishery_pond, mushroom_farm.
- [x] sugarcane_farm_auto — water now adjacent to the sand at the soil's own level (y=1) so canSurvive passes (cane no longer pops on print); observers raised to the cane's 2nd block for correct harvest; snapped cane → water → hopper → chest.
- [x] super_smelter — full row of input chests, each with a down-hopper feeding its own furnace; fuel + output extended across the whole bank so all 5 furnaces run in parallel.
- [x] map_room — filled the 4 corner gaps (potted cornflowers at the entry, barrels by the cartography table).
- [x] mineshaft_entrance — was a sealed facade; opened a 2-tall walk-in tunnel mouth + rails leading in + an open dig-down shaft start at the back (surface entrance marker).

---

## Deploy + verification (all 8 reported items done, 2026-06-16)
All 8 open items above ✓ fixed/committed/pushed. Rebuilt `mc3dprint-0.4.0.jar`, **redeployed** to the Prism 1.20.1 instance (replaced in place, checksums match, no duplicate). `./gradlew build` + `runGameTestServer` green — **all 69 gametests pass, 132 blueprints**. No regressions.

## Cascade analysis (per Patrick's request — "do these problems hit other builds I haven't tested?")
Grounded in the foundation audit (`-DauditFoundations=true`) + a 3-way read-only code review of farms, raised entrances, and multi-level access.

**A) Did MY changes regress anything? No.** The global changes are isolated or net-positive:
- `grass_block` now FU-valued (1@1) → builds that use it now print their grass floors/footings instead of dropping them (fixes the floating class; **foundation audit now flags 0 floating builds**). It's the lowest tier so it can't bump any build's disc tier; abundance-safe to wind (dirt-tier).
- Deterministic block-entity serialization + printer `sendBlockUpdated()` on place → signs/containers sync correctly; no gameplay change to other builds.
- Per-build geometry fixes are local. 69/69 gametests + 132 blueprints confirm no regression.

**APPROVED PLAN (2026-06-16):** fix wizard_tower + library + nether_wart_farm cosmetic now; mushroom_farm → honest semi-auto (like tree_farm); make castle_keep top, barn hayloft, AND church belfry reachable; animal_pen → add an empty chest only (no stocked spawn eggs). stable_horse stays a stair. Then rebuild + redeploy the jar again.

**B) Other builds with the SAME bug classes you suspected — yes, a handful (now being fixed per the approved plan):**

*LIKELY-BROKEN (objective bugs):*
- [x] **fantasy_wizard_tower** — FIXED: added a deepslate-bricks backing spine behind the ladder (y1-8, y10-16; the y9/y17 deck discs already back it), so the ladder places and the climb reaches the observatory deck. — the climb ladder was **unbacked**: it's `facing=north` up the center of a round tower, so the cell behind it is interior air the whole run → the ladder has no support face and **pops off on print (won't place)**. You literally can't climb the tower. Fix: add a solid backing spine behind the ladder (clear of the y9/y17 hatches), or hug the ladder to the ring wall at each taper. *(Worst one.)*
- [x] **library** — FIXED: extended the mezzanine ladder to the deck layer (y=1..4, no more 2-block mantle) AND added a dark_oak_planks backing post at (7,1..3,8) so the previously-unbacked lower rungs have a real support face (no longer relying on suppress-drops). — mezzanine ladder ran only y=1..3 but the gallery walking surface is y=5 → a **2-block mantle** with nothing to grab (ladder ends 2 short). Fix: extend the ladder to fill the hatch at the deck layer (y≤mezzY).
- [x] **mushroom_farm** — FIXED: tore out the fake observer/piston/water harvester; rebuilt as an honest dark grow chamber (sealed roof = no skylight, mycelium floor, spaced red+brown mushrooms, door + chest). Mushrooms spread on dark mycelium; player reaps & replants. Sign + javadoc corrected to describe the spread mechanic (chamber is deliberately too short for giant mushrooms). — was fake auto-harvest (observers watch mushrooms that never change state in place).

*SUSPECT (likely needs a decision, not clearly a bug):*
- [x] **castle_keep** — FIXED: added a stone-brick rooftop deck at y=14 across the keep interior (hatch left open at the ladder column) + extended the ladder to y=14, so it climbs flush onto a crenellated battlement (ringed by the y=15 merlons). Great-hall chandelier below untouched. — was a ladder to nowhere (hollow open-top tower).
- [x] **church** — FIXED (you chose reachable): opened a 2-tall base doorway from the nave into the steeple (2,1-2,1), ran a ladder up the 1×1 shaft (1,1-5,1) backed by the north wall, relocated the glowstone out of the shaft, removed the belfry plank floor, and re-hung the bell ceiling-attached at (1,7,1) (anchored to stone at (1,8,1)) so it's ringable from the top rung. — was a sealed stone shaft.
- [x] **barn** — NO FIX NEEDED (analysis was wrong). Re-verified against `pillar(b, x, z, y0, y1, mat)`: `pillar(b,7,5,1,2,HAY)` is x=7/z=5/**y=1..2** — the hay is floor-level corner bales, NOT floating at y=5 (the audit agent misread the signature). Dump confirms hay only in y=1/y=2; y=4-6 are pure roof. It's a sound single-storey barn (walls y=1..2, gambrel roof y=3..6 — no vertical room for a loft, and none needed). Left as-is.
- [x] **nether_wart_farm** — FIXED: consolidated the brewing station into the NE corner (capping the x=6 walkway dead-end) + moved the chest to cap the x=3 dead-end; restored the deleted wart cells so all 3 beds are whole; walkways clear end-to-end. — was: a `water_cauldron` overstamped a crop cell + NE furniture boxed in the walkway.

## Pre-retest sweep — unbacked ladders (2026-06-16)
Before the next in-game test, swept ALL 132 builds for the unbacked-ladder class (a ladder prints via the printer's `UPDATE_SUPPRESS_DROPS` even when its support cell is air, then **pops on the next block update** — a SILENT failure the GameTests don't catch). Added a reusable gated audit: `./gradlew test --tests '*BlueprintLadderSupportAuditTest*' -DauditLadders=true --rerun-tasks` → `build/blueprint-ladder-audit.txt`. It found **6 builds the manual multi-level audit had wrongly cleared** (that audit checked the *facing* side, not the *support* side = `facing.getOpposite()`). All fixed; audit now 0-flagged:
- [x] copper_clocktower / purpur_tower — ladder `facing=north` mounted against the NORTH wall (support side z+1 was air); flipped to `facing=south` so it attaches to the wall.
- [x] guard_tower — ladder was dead-center (air on all sides); relocated to the N-wall corner column `facing=south` + moved the deck hatch to match.
- [x] mangrove_stilt_hut — backing post is SOUTH of the ladder but it was `facing=south` (attaches north=air); flipped to `facing=north`.
- [x] sailing_ship — quarterdeck ladder refaced to back onto the raised-deck plank; hold ladder moved off the centerline onto the hull side (a ladder can't hang off a deck underside).
- [x] victorian_townhouse — the wall courses skipped the inter-story floor levels, leaving 2 rungs unbacked; added matching brick backing at those cells.

Root cause across most: the `facing` was set TOWARD the backing wall instead of away from it (a ladder attaches to `facing.getOpposite()`). Config verified current (game-instance `mc3dprint-common.toml` already has the grass_block/mycelium FU values), jar rebuilt + redeployed.

*Confirmed OK (spot-checked, no action):* villager_trading_hall, fishery_pond, koi_pond, small_farm, bee_apiary, scarecrow (farms); fishing_hut, japanese_tea_house, railway_station, victorian_townhouse (the buried-door audit flags are all **legit reachable raised entrances**, not traps); lighthouse, watchtower, guard_tower, copper_clocktower, purpur_tower, manor_house, victorian_townhouse, copper_observatory, windmill, japanese_pagoda (multi-level access all sound). **No stair-facing bugs of the tavern/stable kind remain** — every other interior climb is a ladder; the failures above are backing/run-length/destination, not facing.

---

## Retest round 2 (2026-06-16, second in-game pass)
PGMac retested in-game; this round's findings + the audit-surfaced extras. **Built 3 of the requested guardrails as gated audits** (plant-support, reachability, bed-clearance) — see the `blueprint-qa-audits` memory — and they surfaced more instances than were spotted by eye.

**STAIR FACING CONVENTION CORRECTED:** I had it backwards. A stair **ascends toward its `facing`** (raised step on the facing side), so a northward climb = `facing=north`. (My earlier "tall riser opposite facing" was wrong; PGMac confirmed `facing=south`-for-northward read backwards on both tavern_inn and stable_horse.)

- [x] tavern_inn — flipped the 6-step climb to `facing=north` (kept the position/hatch/landing fixes).
- [x] stable_horse — replaced the (mis-oriented) loft stairs with a wall-backed **ladder** up the west-middle stall (PGMac preferred a ladder — more authentic); ladder audit 0-flagged.
- [x] mushroom_island_hut — threshold step into the hut: the agent set `facing=north` (riser toward the entering player → still blocked); corrected to `facing=south` (player enters walking south).
- [x] greek_quartz_temple — dropped the useless solid bottom plinth (verified empty) and re-based down so it's enterable; also fixed a latent sealed cella door (`set(air)` no-op).
- [x] elven_treehouse — bed was embedded (glass over the foot + wedged in the wall); relocated into open floor with headroom. Bed audit clean.
- [x] modern_glass_villa — was sealed on both floors; added a ground-floor door + a backed ladder to the 2nd floor. Reachable.
- [x] copper_clocktower — sealed; added a base door into the shaft (climb the existing ladder up).
- [x] bee_apiary — flowers were on planks (floated); laid grass_block under each → garden beds.
- [x] cherry_grove_cottage — pink_petals on planks → moss_block patches.
- [x] koi_pond — sugar_cane on stone_bricks → dirt + adjacent water.
- [x] cemetery_plot — dead_bush on a wall → relocated to ground.
- [x] greenhouse — 2 stray crops sat on corner lanterns → removed (bed layout intact).
- [x] diamond_vault — iron door opened into a solid inner wall (sealed); carved a real doorway + opened the cage so the chamber is enterable.
- [x] sailing_ship — cabin roof-hatch sat over a wall → recentered it; below-deck bilge is intentional (allowlisted).

**Guardrails now in place (gated audits):** `-DauditFoundations`, `-DauditLadders`, `-DauditPlantSupport`, `-DauditReachability` (hard-throws, with an intentional-seal allowlist), `-DauditBeds`. All currently clean (bed audit's only flags are iron_farm's by-design villager beds). 69 gametests green, 132 blueprints, jar rebuilt + redeployed.

**Recurring bug class noted:** `Builder.set(pos, AIR)` is a no-op, so "carving" an opening by overwriting a wall with air leaves it solid (door/hatch into stone). Fix by skipping those cells in the wall loop. Hit on greek/copper/villa/diamond_vault/sailing_ship — the reachability audit catches it.

---

## Retest round 3 (2026-06-16, third in-game pass) — NO jar redeploy (Patrick actively testing)
New bug class: **a fire source (raw `lava`/`fire`) next to flammable blocks (wood/wool/leaves/etc.) ignites and burns the build down.** "Boxing" lava in cobble is NOT enough — lava ignites flammables a couple blocks away. Safe heat sources near wood: campfire, lava_cauldron, magma_block, torch/lantern (none spread fire).
- [x] **tavern_inn** — DONE: campfire hearth replaces the lava firebox (campfires don't spread fire); cobble surround + grate + chimney kept. — the caged-lava hearth burned the whole (spruce) tavern down. Replace the lava firebox with a **campfire** (no fire spread); keep the cobble surround + chimney + grate.
- [x] **flammable-near-fire guardrail (audit built)** — `BlueprintFireHazardAuditTest` (`-DauditFireHazard`) flags raw `lava`/`fire` within Chebyshev≤2 of flammable blocks. Convention documented in qa-architecture §3. Found 2 real hazards (below); iron_foundry lava is SAFE (encased in deepslate/iron, no wood). Promote to always-on gate after the 2 fixes.
- [x] **blacksmith** — DONE: forge lava → magma_block (glowing hot bed, no fire spread).
- [x] **smithy_workshop** — DONE: forge lava → lava_cauldron (contained molten crucible, no fire spread).
- [x] **fire-hazard audit promoted to always-on gate** — 0 flagged; raw lava near flammables now fails the build.
- [x] **modern_glass_villa** — DONE: cleared the smooth-quartz slab the entrance-fix left in the door approach; flat walk-through.
- [x] **copper_clocktower** — DONE: lowered interior shaft floor y=1→y=0 (flat walk-in to the shaft, then climb the ladder).
- [ ] **CORRECTED THRESHOLD RULE (Patrick):** a stair just inside the door does NOT work — your head hits the door's upper half stepping up. The fix is a FLAT walk-through: the cell you step into must be at the SAME level as the door sill (leave it empty / level it), with 2-block headroom through the open door. Applies to mushroom_island_hut (the stair I added is wrong → flatten) + copper + any raised-floor entry.
- [x] **mushroom_island_hut** — DONE: removed the wrong threshold stair; cleared to a flat mycelium walk-in at sill level.
- [x] **navigability gate — flat walk-through enforced** — every door must walk in flat at sill level (≤½-block slab/drop-in OK; full step-up or blocker fails). Flagged + fixed 7 builds (copper, mushroom_island_hut, modern_glass_villa, purpur_tower, mushroom_farm, redstone_workshop, barn); fence-gates/slab/carpet thresholds correctly NOT flagged. Always-on gate, green.
- [x] **bee_apiary** — DONE: removed the chains; the two aisle lanterns now hang directly off the y=4 honeycomb beam (at y=3, one block above head) so the aisle is walkable.

---

## Retest round 4 (2026-06-16, fourth in-game pass) — NO jar redeploy (Patrick actively testing)
New bug class: **a hanging lantern under a non-sturdy support (a stair/slab) pops off on print** — same attachment-support family as the unbacked-ladder class. A `lantern[hanging=true]` needs the block above it to present a sturdy DOWN face (full block, or a chain — chains float + support center). Top-half stairs / slabs do NOT, so the printer places the lantern via suppress-drops and it drops on the next block update.
- [x] **japanese_pagoda** — DONE: the 16 eave-corner lanterns hung directly under the upturned-corner bracket **stairs** (top-half `dark_oak_stairs`) → all broke off on print. Each now hangs off a **chain** (chain at ey-2, lantern at ey-3), mirroring the build's own interior soul-lantern. Interior soul-lantern was already fine.
- [x] **(guardrail) BlueprintHangingLanternAuditTest** — DONE: always-on gate. A hanging lantern uses CENTER support, so it flags exactly the cases that genuinely fail — **air/OOB above, a top-half stair, or a top slab** (fences/walls pass: their centre post covers the column). 0-flagged now; fails the build on any future break.
- [x] **iron_farm — zombie cell** — DONE: rebuilt as a FULLY ENCLOSED 1×2×3 glass box on the east wall (interior x=11/z=5-6, walled y=10..12 on every deck-facing side, stone corner posts, perimeter east wall, roof cap). The zombie can't step out onto the deck and fall now; glass keeps villager line-of-sight; the 1×2 fits a boat. Verified in the dump; reachability stayed green.
- [x] **iron_farm — wash water floods the room** — DONE: a 2×2 layer of **wall signs** in the drop mouth (y=3, each attached to the surrounding ceiling brick) dams the wash water at the ceiling — shaft above fills, collection room (y=1..2) stays dry, golem/items still fall through. Verified in the dump.
- [x] **(cascade) 5 more builds with the unsupported-hanging-lantern class** — DONE (surfaced by the new audit, none were tested in-game): **japanese_tea_house** (8 eave lanterns under top-half stairs → chain, identical fix to the pagoda), **gazebo** (4 floating → chain at the y=4 air cell above each), **fishing_hut** (2 porch lanterns floating → chain at the eave course), **blacksmith** + **railway_station** (lanterns under top slabs → solid anchor block re-stamped above, flush with the slab roof, keeps the lantern at height). Audit 0-flagged.
- [x] **mob_xp_tower — REIMAGINE (Patrick)** — DONE: reworked the spawn deck. A solid BASIN floor (y=18, 7×7 minus the central 3×3 hole) holds a 1-deep water FRAME (y=19) fed by 4 sources at the arm mid-edges that flow INWARD and drain down the open 3×3 hole; four DRY 2×2 corner PADS (y=19) are the only spawnable surface. Mobs spawn on the dark pads, step off into the flushing frame, get swept to the hole and down the shaft. Also made the roof a FULL stone block (slabs leak skylight) so the chamber stays light-0. Verified in the dump; build + 76 gametests green. *(was: drop hole never punched + flat water pool that won't sweep and mobs won't spawn on)* (the 2×2/3×3 "hole" cells are solid, so mobs can't fall to the kill chamber — Patrick punched one by hand to confirm intent); (2) the platform water just sits in a pool — mobs won't spawn on water AND a flat pool won't sweep them anywhere. Rethink the whole spawn-deck → drop → kill flow: real open drop hole, DRY spawnable platform with water that actually flushes mobs into the hole (sources at the rim flowing inward to the single low point), or an alternative that works. Screenshots: water pooling on the deck + sitting in the corners.
- [x] **pumpkin_melon_farm — REIMAGINE from scratch (Patrick)** — DONE: rebuilt 9×9×4. The flood is gone — the ONLY water is a single 1-wide channel down the centre (x=4, y=1) on a hopper line → chest. Stems are OUTER (x=2,6), the dirt growth blocks INNER (x=3,5, beside the channel) so a broken gourd drops into the channel → hopper → chest. (Pushed back on the vertical sticky-piston auto-harvest: the dirt growth block sits *between* the fruit the observer must watch and the piston below it, so there's no clean observer→piston link — it needs fragile multi-block redstone that won't reproduce in a compact print. Shipped the robust minimal-water semi-auto instead.) Verified in the dump; build + 76 gametests green.
- [ ] ~~pumpkin_melon REIMAGINE detail~~ — current build is "massively screwed up / useless" (screenshot: a big flat water pool, no working mechanism). Redesign. Patrick's suggested design: sticky pistons UNDER the dirt growth blocks; when a pumpkin/melon grows onto an adjacent dirt block, the observer (watching the grow cell) fires → pulse routed DOWN → triggers the sticky piston to shoot UP into the block the fruit formed on, breaking it → fruit drops to a water sweep → hopper → chest. Build around that piston-up-break mechanic (the earlier "sideways growth + flowing-water sweep" wasn't enough).
- [x] **sugarcane_farm_auto — cane breaks on one side + needs a full hopper floor (Patrick)** — DONE: (1) removed the y=2 water (the cane's OWN level — it flowed into the not-yet-settled cane during the print and washed a row out); kept the single y=1 water line beside the soil so canSurvive still passes ("water down one layer"). (2) Replaced the single end-hopper with a full hopper LINE under the whole channel (x=4, z=1..7) all facing south — sucks cut cane from the water above wherever it lands and chains it to the chest. Verified in the dump; build + 76 gametests (incl. sugarcane collection routing) green. *(original two problems below)*
  - two problems: (1) the cane on one side broke the instant the build finished, BEFORE the water was placed (player can manually replant and it holds → likely the water is one layer too high / on the cane's level so the cane pops, or the soil/water relation is off — water should be one layer DOWN from the cane's planting level so it irrigates without touching the cane stalk); (2) there's no hopper floor under the water channel — harvested cane floats down the water and FALLS THROUGH. Need hoppers across the WHOLE bottom (under the water), all pointing toward the front chest, so cut cane routes to the chest instead of dropping out. Screenshots: cane on one side, underside showing no hoppers.
- [x] **cactus_farm — REIMAGINE from scratch (Patrick)** — DONE: rebuilt as a compact 5×5×4. The flooded pool is gone — two cacti 2 apart, ONE shared breaker between them, a tight 7-cell water MOAT (just rings the bases so segments land in water, not on the cactus that would destroy them), and a connected HOPPER FLOOR under the whole moat that sinks the catch and chains it to the chest. Verified in the dump; build + 76 gametests (incl. cactus collection routing — re-fixed when the first moat-only version isolated the water) green. *(original detail below)*
- [ ] ~~cactus REIMAGINE detail~~ — "a certified mess… WAY too much water," useless. Cactus needs: sand grow-blocks each with the 4 horizontal neighbors EMPTY (cactus breaks if any side block is adjacent), a breaker (a block) at the cactus's grow-into height so the grown segment pops, and the drop routed to a hopper→chest. NO big water pool — cactus collection is usually a hopper directly under the breaker, not a water sweep. Keep it compact and dry.

## Retest round 5 (2026-06-16, fifth pass) — NO jar redeploy (Patrick actively testing again)
- [x] **cactus_farm — water in wrong spot + center hopper → water (Patrick)** — DONE: dropped the collection a level — a 3×3 HOPPER FLOOR now sits at y=0 (below the sand), so the water sits at the SAND LEVEL (y=1) with the CENTRE cell as water over the drain (the old centre hopper moved down to y=0). A cobble RIM contains the water (no spilling), gap above the chest. Snapped segments land in the sand-level moat → sink to the hopper floor → chest at the south edge. (Cactus stays player-planted — it's unvalued/unprintable like bamboo/kelp; sign says PLANT.) Verified in the dump; build + 76 gametests green.
- [x] **mediterranean_terracotta_villa — lantern blocking the front porch entrance** — DONE: removed the central pergola hanging lantern at (cx,2,0) which sat dead-center on the door approach at head height; rafter beam restored underneath. Entrance clear (verified in dump). Interior + wall lanterns still light the porch.
- [x] **pumpkin_melon_farm — AUTOMATE (Patrick, opted in to the bigger build)** — DONE: rebuilt 9×9×5 with a TIMER-driven vertical harvester. Stems sit on hydrated farmland (x=2/6); fruit grows onto the INNER dirt blocks (x=3/5), each on a `sticky_piston[facing=up]` at y=1. A self-starting face-to-face OBSERVER CLOCK at the north end ((3,1,0) east + (4,1,0) west) pulses two redstone-dust buses (x=2/6) that fire every piston, shoving the dirt UP into the formed gourd to break it. The ONLY water is the central channel (x=4, y=1+2) on a hopper line → chest, with a GLASS cap over the chest (4,1,8) so it can't spill. Verified the clock/piston/channel geometry in the dump. *(a) fruit-destroy = vertical pistons; (b) glass over chest — both done. The clock is fast; a repeater in a bus slows it (noted on the in-build signs).* **In-game verify: redstone timing + whether the up-push breaks vs relocates the gourd (gametests only check collection routing).**
  - (a) missing the fruit-DESTROY mechanism — use sticky pistons UNDER the dirt growth blocks pushing UP to destroy the formed fruit; a TIMER (clock) is acceptable instead of a detector; much-larger footprint is fine. (b) glass above the chest to stop water spilling out (screenshot).
- [x] **sugarcane_farm_auto — redstone harvest mis-synced (Patrick)** — DONE: scrapped the per-cane observer→piston wiring for the SAME timer/clock harvester as pumpkin/melon. A self-starting face-to-face observer CLOCK at the north end ((2,3,0) east + (3,3,0) west) pulses two dust buses (x=1/7) that fire a full row of pistons at the cane's 2nd-block height (y=3) — each piston (x=2/6, facing the cane) punches the grown cane toward the central channel; the always-present bottom block regrows. No vertical-signal climb (the old failure): the dust bus sits directly BEHIND each piston at the same y=3. Also fixed a latent bug — the old south end cap put cobble ON TOP of the chest (4,1,8), blocking it; the chest top is now left open. Verified the clock/piston/bus geometry in the dump. **In-game verify: redstone timing (gametests only check collection routing).** mob_xp_tower confirmed WORKING last round (nice).
  - two coupled faults: (1) observer fires only on the FIRST growth — it watched the 2nd-block cell (y=3) but the piston broke the 3rd block (y=4), so the watched cell never reset; (2) the observer back-powered the stone shelf but the dust strip sat 2 blocks above and a powered block doesn't carry the signal up.
- [x] **iron_farm — golem doesn't die (lava cauldrons too small)** — DONE: implemented Patrick's LAVA BLADE. y=1 2×2 core is now HOPPERS (golem stands here); a 2×2 LAVA sheet sits at y=4, held up by the y=3 dam signs (a sign blocks fluid but passes the golem's head + the iron drops); the standing golem's head (~y=4.7) burns in the lava → dies; iron drops fall below the lava to the hoppers → ring → chest. Added the y=2 4×4 containment walls so the golem can't path off the blade (sealed kill column). Added a SECOND 2×2 sign dam at y=8 so the spawn wash-water stops 4 courses above the lava (no obsidian). Allowlisted iron_farm in the fire-hazard gate (the dam signs sit 1 cell from the lava but the nearest AIR is 2 cells away — below fire's ~1-block reach, so they don't ignite — the standard lava-blade reason) and in the reachability gate (kill column + shaft/spawn enclosure + zombie cell are all intentionally sealed). Verified the full stack in the dump; build + 76 gametests green. (round-4 deploy summary below)

## Status: ALL round-4 items DONE + jar redeployed (2026-06-16)
Every round-4 finding is fixed, committed, and pushed. Rebuilt `mc3dprint-0.4.0.jar` and **redeployed** to the Prism 1.20.1 instance (replaced in place, checksums match, exactly one jar — no duplicate). `./gradlew build` (all always-on gates incl. the new hanging-lantern gate) + `runGameTestServer` green — **76 gametests, 132 blueprints**. No FU/economy changes this round (all geometry), so no config deletion needed. Ready for the next playtest pass.

---

## Retest round 6 (2026-06-17) — "done testing for now": finish the queue, then a global change + redeploy
- [x] **sailing_ship — bigger/taller + real basement + better sails (Patrick)** — DONE: full rebuild from a 5×13×14 sloop to a 7×19×18 three-masted galleon. NEW: an enclosed 3-tall below-deck **HOLD** (the basement) reached by a WORKING hatch+ladder — a hull-backed `ladder[facing=west]` runs y=2..5 in an open deck hatch at (5,·,8), dropping you to the hold floor (the old hold ladder floated on the deck underside, reaching nothing). Hull tapers from a wide flat transom to a pointed bow; stern **captain's cabin** with a real walk-in oak door (doorway left open in the wall loop, not carved); ship's wheel; raised forecastle; **three masts** (mizzen/main/fore) with big CLEAN rectangular wool sails (no ragged belly offsets) framed by gunwale shrouds; bowsprit+jib; deck cargo. All lanterns are RESTING (no hanging-support risk). Verified the full stack in the dump (hull/hold/hatch/cabin/masts/sails). `sailing_ship` stays in the reachability allowlist (hull voids intentional). *(was: didn't look like a ship, poor sails, for-show ladders, too small.)*
- [x] **underwater_dome_base — iron door needs a trigger + missing block over the door (Patrick)** — DONE: an iron door can't be hand-opened, so flanked it with two **stone buttons** on the west jamb — one OUTSIDE (north face, to enter) and one INSIDE (south face, to leave); each powers the jamb adjacent to the door so a press swings it. Also capped the **gap above the door** (the dome shell curves inward by y=4, so the door pokes out past it) with an iron **lintel** at (5,4,1), completing the framed portal. Verified in the dump (buttons at (4,2,0)/(4,2,2), lintel at (5,4,1)).
- [x] **rename `conduit_shrine` → `underwater_conduit_shrine` (Patrick)** — DONE: renamed the build key, method, display name ("Underwater Conduit Shrine"), javadoc, and `@link` across `CuratedBlueprintGenerator`; the `CURATED_NAMES` entry in `CuratedBlueprints`; the two docs (`phase2-build-tracker`, `blueprint-candidates`); and the resource file (`conduit_shrine.blueprint` → `underwater_conduit_shrine.blueprint`). No lang/patchouli/loot reference exists (the disc name lives in the blueprint NBT). Regenerated the new-named file.

### Global change (all blueprints): Print Cost on the disc tooltip (Patrick)
- [x] **"Print Cost: NNN Tier X FU"** — DONE: the blueprint disc now caches a print cost (NBT `PrintCost`, written in `writeBlueprint`) and shows it on the tooltip under the existing Tier line. The number is the blueprint's TOTAL FU **converted into the single highest tier present** (what it costs if you only feed a top-tier spool): sum each priced block's base units (`fu × ratio^(tier-1)` × count, mirroring the printer's free-block rules — itemless/structural and unpriced blocks contribute 0), then ceil-convert the total to the top tier (`FuConversion.fromBaseCeil`). It's the BASE cost (machine-independent / max-efficiency break-even); the per-machine efficiency markup isn't included since the disc doesn't know the printer. Added lang key `tooltip.mc3dprint.disc_print_cost`. Compiles in the regen pass.

**Status: DONE + redeployed (2026-06-17).** `./gradlew build` (all always-on gates) + `runGameTestServer` green — 76 gametests, 132 blueprints. Rebuilt `mc3dprint-0.4.0.jar` and **redeployed** to the Prism 1.20.1 instance (replaced in place, checksums match, exactly one jar — no duplicate). No FU/economy values changed (Print Cost only reads them), so no config deletion needed. **In-game verify (redstone can't be gametested):** pumpkin/melon + sugarcane clock timing & whether the up-push breaks the gourd; the dome buttons opening the iron door; the ship hold ladder climb.

---

## Retest round 7 (2026-06-17, sixth in-game pass) — NO jar redeploy (Patrick actively testing)
- [x] **mushroom_farm — redstone-torch lighting (Patrick: "look up if redstone torches are too bright; if not, use them, it'll look cleaner")** — DONE. Verified: a redstone torch is **light level 7**, under the **≤12** ceiling small mushrooms need to be placed/spread on normal blocks — and on the chamber's MYCELIUM floor mushrooms survive AND spread at ANY light level anyway, so the lighting can't fizzle the farm. (A plain torch/lantern at 14–15 WOULD be too bright on normal ground; redstone torches are the safe, on-theme pick. Light doesn't stack → a full ring still tops out at 7/cell.) Added a ring of `redstone_wall_torch` at y=2 on the interior walls (north x=2/4/6, west/east z=2/4/6, south corners x=1/7), each backed by solid wall (no pop-off), clear of the door/chest/sign. Was a pitch-black box; now a clean red glow. Updated the javadoc (dropped the old "NO interior torches" claim) + the sign ("dim + mycelium"). Verified placement in the dump; build (incl. printability — redstone_wall_torch derives FU from its recipe) + 76 gametests green.
- [x] **chicken_coop_auto — REBUILT, the cooker didn't work (Patrick)** — DONE. Two real bugs: (1) it used `lava_cauldron` blocks as the pen wall, but a cauldron holds its lava RECESSED behind a full-block face → a chicken beside it never touches the lava ("no way to get to the lava"); (2) input was a cramped 1-block side gap ("no way to drop a chicken in"). Raw lava can't replace it — a chicken is only ~0.7 tall so it can't reach lava one cell above (the iron-farm head-in-blade trick needs a TALL mob), and lava in the chicken's own cell burns up the cooked drop. **Rebuilt around a CAMPFIRE kill floor:** a lit campfire on every hopper of a 5×5 floor cooks any chicken standing on it (fire death → COOKED drop), campfires DON'T destroy dropped items, and the hopper below pulls them to a front-edge chest (glass cap → still opens). **OPEN TOP** for input (drop chickens / throw eggs in — matches the egg test). No raw lava/fire → nothing spreads/leaks. Updated the `chickenCoopRoutesToChest` gametest (chest moved (3,1,5)→(3,1,6); drop cell (3,2,3) is now a campfire — items survive on it). Verified the dump; build + 76 gametests green. **Switched lava→campfire deliberately** (the reliable cooked-chicken mechanism) — tell me if you specifically want a lava look and I'll do a lava drop-shaft variant. **In-game verify:** chickens cook + collect; whether the open-top input is convenient enough (vs a ground-level herd-in gap).
- [x] **dragon_statue — revamp: "too abstract, make it way more impressive + much larger" (Patrick)** — DONE. Full rebuild from a crouching 7×11×11 (157 blocks) to a **REARING dragon guardian, 15×22×17 (~927 blocks, ~20 tall)** on a stepped plinth: haunches → forward-arcing chest/neck → roaring horned head (open maw, glowing sea-lantern eyes, end_rod horns + dorsal crest, chain whiskers), **two great wings spread to the full wingspan** (membrane fans sweeping from the shoulders out to x=0/x=14 and up to y≈20, polished-deepslate leading-edge bones + end_rod finger claws), raised clawed forelegs, and a tail curling at the rear. Built SOLID (no hollow interior → no enclosed-air reachability flag; the only cavity is the open maw venting forward). Same proven dark palette (blackstone/deepslate/coal_block + sea_lantern/end_rod/chain) — no dark_prismarine, no panes, no falling blocks. Verified the silhouette layer-by-layer in the dump; build (all gates) + 76 gametests green. *(Aiming for the Grand Library bar — scale + verticality + detailing.)*
- [x] **cottagecore_cottage → rename + entrance slab (Patrick: "weird name" + "half slab blocking the entrance inside the door")** — DONE. (1) Removed the mossy-cobble doorstep slab at (3,1,1) just inside the door — it was a half-step-up that blocked the walk-in; the cell is now flat at floor level (verified air in the dump). (2) Renamed **`cottagecore_cottage` → `storybook_cottage`** ("Storybook Cottage" — matches the build's own "storybook perennial cottage" javadoc): build key, method, display name, javadoc, `CURATED_NAMES`, docs, and the resource file. No lang/loot/patchouli refs. **Picked the name "Storybook Cottage"** — say the word if you'd prefer a different one (one-line change).
- [x] **library (Grand Library) — 2nd-storey walkway blocked + EXEMPLAR note (Patrick: "by far one of the best… exceptional… looks incredible")** — DONE. The mezzanine arms were only 2 cells deep (bookshelf on the outer cell, fence rail on the inner cell) → no room to stand. **Bumped each gallery arm to 3 deep and moved the rail to the new atrium lip**, so every arm now reads **bookshelf (outer wall) | walk lane | fence rail**: west bookshelf x=1 / walk x=2 / rail x=3; east x=9 / x=8 / x=7; south z=9 / z=8 / z=7. Atrium stays open (x=4..6, z=1..6, double-height); ladder access intact. Verified the gallery + rail + walk lanes in the dump; build + 76 gametests green. **Logged the Grand Library as an exemplar reference** in the `exemplar-builds` memory (the bar to aim for on other revamps).
- [x] **fabricator/printer GUI — add spool slots (Patrick)** — DONE. Previously the only way to add/remove docked spools was Shift+Right-Click on the block; the GUI showed "Spools X/Y" but had no slots. Added a **2×2 spool-slot grid on the bottom-right** (`PrinterMenu`: SPOOL_SLOT_X=178, rows at y=152/174 aligned with the bottom two inventory rows), wired to the BE's existing `spoolInventory()` handler (SpoolItem-only, 1/slot). Shift-click now routes spools to these slots (was `attachSpool`); shift-click out → inventory. **Slot count follows the tier** (T1=1, T2=2, T3=3, T4–T8=4) — `PrinterScreen` draws a well under each LIVE slot (reusing the baked upgrade-well sprite) so lower tiers show **fewer** wells, not empty ones. Made the **"Spools X/Y" label smaller (0.85×) and moved it down** to sit just above the grid (≈2nd inventory row), leaving empty space below the upgrade column **reserved for the next feature** Patrick wants to add. compile + build (all gates) + 76 gametests green. *(No texture regen — wells are drawn dynamically.)*
- [x] **creative tab — move farm discs to the bottom (Patrick: "so I can find them easier")** — DONE. `ModCreativeTabs` now emits non-farm curated discs first, then a `FARM_BUILDS` set last (iron_farm, mob_xp_tower, the 5 crop farms, villager_trading_hall, animal_pen, chicken_coop_auto, fishery_pond, tree_farm, mushroom_farm, nether_wart_farm, bee_apiary, super_smelter, small_farm) — so the often-tested farms group at the bottom of the MC3DPrint tab.

---

## Retest round 8 (2026-06-17) — NEW Redstone Clock block + farm-redstone overhaul (one big pass, then redeploy)
**New block: `mc3dprint:redstone_clock`** — an autonomous, SILENT timer: once placed it pulses redstone from all 6 sides every N seconds (configurable 1–60 in a small GUI, never negative, no sound). The interval persists in NBT and round-trips through scan→disc→print (`IntervalSeconds`), so a blueprint can bake a per-build rate. Crafted from vanilla parts (redstone + 2 redstone torches + a clock); FU derives from the recipe (≈T3). Block + BE + menu/screen + registration + recipe + blockstate/models + generated texture + lang + creative-tab entry. Built to replace the fire-too-often paired observers in the farms.
- [x] **Redstone nuance fixed on ALL piston farms:** a straight dust line running past a piston does NOT power it — only a REPEATER pointed into the piston does. Every farm's bus now runs one column out with a repeater into each piston.
- [x] **sugarcane_farm_auto** — Redstone Clock (60s) + repeaters-into-pistons + **glass cap over the chest** (stop water spill). (round 7)
- [x] **pumpkin_melon_farm** — Redstone Clock (60s) + repeaters into the up-pistons.
- [x] **bamboo_farm — REBUILT (Patrick: "several layers of water for no reason")** — was flooding y=0+y=2+y=3+y=4. Bamboo needs NO water to grow, so rebuilt 7×7→**9×9** mirroring sugarcane: water ONLY as a single base-layer transport canal (y=1), Redstone Clock (60s) + repeaters + pistons, mud grow-blocks (player plants bamboo), glass chest cap. Updated the bamboo collection gametest coords.
- [x] **kelp_farm — clock + repeaters + floating-item investigation (Patrick)** — replaced observers with the Redstone Clock; pistons now repeater-driven (dry, outside the glass). **Investigated floating kelp:** cut kelp (like all items in water) floats UP, so a bottom hopper can't catch it. Confirmed there's no compact, reliably-printable vanilla skimmer (a surface flowing-water push to a drop hole works in principle but doesn't settle deterministically on print). Per Patrick's fallback → **honest SEMI-AUTO**: the clock auto-cuts, the player scoops the floating kelp off the open top and smelts it (signs say so). Kept a bottom hopper→chest for routing validation. **No modded vacuum hopper.** [Sources: [Bubble Column – Minecraft Wiki](https://minecraft.wiki/w/Bubble_Column)]
- [x] **cactus_farm — 4 sand + AUTO-PLANT (Patrick)** — rebuilt 5×5→**7×7** with **4 cacti** (spaced 2 apart so none break each other) and the printer **auto-plants the cactus** (placed at y=2, above the y=1 water, so it prints right after the water). **Economy change:** cactus was intentionally unvalued (unprintable, like bamboo/kelp) — gave it a small FU value (`cactus=2@1`, lowest tier) + **winder-blacklisted** it (anti-laundering) so the printer can place it. Each base is air-surrounded (survives); two shared breakers pop the segments → water → hopper floor → chest. Updated the cactus gametest coords. **Deleted stale config** (FU change doesn't merge into an existing toml).
- [x] **chicken_coop_auto — LAVA BLADE (Patrick's experiment)** — the campfire floor killed but didn't COOK (campfire damage doesn't set the on-fire flag → raw drops). Rebuilt as a 5×9×5 lava-blade drop: deck water pushes the chicken to a central hole → it washes down → falls THROUGH a 1-block lava blade (ignites) → lands on a hopper ON FIRE and burns out → COOKED drop → chest. Signs hold the lava + dam the wash-water (no obsidian). **Fire-safe:** the lava has ZERO air neighbours (sign above+below, stone sides) so it can't spread fire; allowlisted in the fire-hazard + reachability gates (like iron_farm). **EXPERIMENTAL — verify in-game:** chickens flap-fall slowly, so one might die IN the lava (burning the drop); if it doesn't cook reliably the campfire version is the fire-safe fallback. Updated the chicken gametest coords.
- [x] **observer sweep (Patrick)** — grepped all `observer[` uses: the only one left is the DECORATIVE redstone-component showcase bench in `redstone_workshop` (observer/repeater/torch/pistons displayed as parts) — NOT a farm timer, so left as-is. All four FARM observers are now the Redstone Clock.
- [x] **printer/fabricator GUI spool slots** + **creative-tab farm grouping** (round 7, above).

**Status: building + redeploying the jar now.** `./gradlew build` (all gates) + `runGameTestServer` → **76 gametests pass, 132 blueprints**. FU/economy changed (cactus valued) → stale config deleted (dev + game instance). **In-game verify (redstone/fluid not gametested):** all 4 clock farms cycle + collect; the cactus auto-plants + self-harvests; the chicken lava blade actually COOKS (the experiment); the Redstone Clock GUI sets 1–60s and persists through a scan→print.

---

## Retest round 9 (2026-06-18) — Resin/clock polish — **NO jar redeploy** (Patrick actively testing)
Fixes are applied to source + committed; the deployed jar is **NOT** rebuilt/redeployed until Patrick signals testing is done.
- [x] **printer/fabricator — stale spool reels after GUI removal** — DONE. Pulling the last spool out **through the GUI** left the exterior reels rendered until relog. Root cause: the BER draws the reels from synced BE data (`clientSpools`), but the `spools` handler's `onContentsChanged` only called `setChanged()` — never a block-update packet — so GUI/hopper mutations updated the open container but not the in-world render. The in-world Shift+RightClick paths (`attachSpool`/`detachSpool`) and `drainFu` synced explicitly; the GUI path didn't. **Fix:** `syncToClients()` now lives in the `spools` `onContentsChanged`, covering GUI, hopper, attach/detach, and drain uniformly; removed the now-redundant explicit syncs in those three methods. (`PrinterBlockEntity`.)
- [x] **resin "Tier I/II/III" → rarity (Common / Uncommon / Rare)** — DONE. Too many overloaded "tier" words (printer/spool tiers are already T1–T8). Converted **display only** — internal registry IDs stay `resin_*_t1/t2/t3` (no world/recipe/loot breakage), the tier int (1–3) still drives everything. Changed: the item tooltip rarity line + color (`ResinItem.rarityKey`/`rarityColor`, new `tooltip.mc3dprint.resin.rarity.{common,uncommon,rare}` lang keys, dropped `resin.tier`); all 11 resin item display names ("(Common/Uncommon/Rare)"); the Patchouli guide (overview "Rarity" page, "Finding Rare Resins", effects/finding body text); README, CHANGELOG, docs/MC3DPrint.md + .html. *(Treasure's common/rare/epic **loot pools** are a separate axis — left as-is, per the distinction Patrick called out.)* **Note for review:** kept the existing tooltip colors (Common=white, Uncommon=aqua, Rare=light-purple); say the word if you'd rather align to vanilla rarity colors (e.g. Rare=aqua). Config-section comments still say "Tier 1/2/3" (internal power-user surface) — left intentionally; easy to flip if you want.
- [x] **Redstone Clock GUI — drop the footer hint** — DONE. Removed the bottom "1–60s · all sides · silent" line from `RedstoneClockScreen.renderLabels` + the now-unused `gui.mc3dprint.redstone_clock.hint` lang key. (The block's hover tooltip still states the 1–60s range — that's the item tooltip, not the GUI footer.)
- [x] **kelp_farm — 4 changes** — DONE. (1) **Missing clock connection** — the two carry-dust cells (z=1) that bridge the Redstone Clock's north connector to the side buses had NO support block beneath (the bus pillars only ran z=2..6, the shelf only z=0), so the dust popped on print and severed the circuit; added a 2-block stone pillar under each. (2) **Auto-plant kelp** — kelp was unvalued → silently skipped; gave it `kelp=2@1` + winder-blacklist (like cactus) and the generator now plants the first kelp layer (y=2) on each grow-block (grows up on its own). (3) **Front rework** — collection chest moved to the FRONT-CENTRE (4,0,8), accessible (replaces the foundation stone that blocked it); fed by the canal bridge hopper AND two furnace under-hoppers (output → centre chest). (4) **Furnaces turned around** — now `facing=south` (front toward player), flanking the chest at (3,1,8)/(5,1,8) on their output hoppers. Regenerated `kelp_farm.blueprint`; kelp gametest chest coord (4,0,7)→(4,0,8); deleted stale config (FU change). *(Verdant making kelp taller isn't supported — the per-block resin can't stack blocks; the planted kelp grows on its own.)*
- [x] **Overdrive cost preview on the disc tooltip** — DONE. With an Overdrive resin in the resin slot, an official blueprint disc's "Print Cost" line is rewritten in place — original struck through (grey), reduced cost beside it (green) — via `PrinterScreen.getTooltipFromContainerItem` + `PrinterMenu.overdriveResinTierInSlot()` + new lang key. Only shows when the cost actually drops (Rare = below break-even; Uncommon = break-even, no base change, left alone). GUI-only (where the resin context lives); the item's global tooltip is untouched.
- [x] **resin footer "used up per print" → "consumable"** — DONE (lang).
- [x] **Quartermaster buffed (felt underwhelming for Rare)** — DONE. Furnaces now share a **64-coal-block budget split evenly** (2 furnaces → 32 each; pre-counted at job start, exact split via running budget). Storage containers share a **64-bread food budget** + **64-torch budget** (split evenly); the enchanted move-in tool kit goes to the **first** chest only (no 5× tools for 5 chests). Brewing stands now get **8 blaze powder** (was 1) + 3 water bottles. Three new tunable knobs in the `resin` config section (auto-merge, no wipe). `ResinEffects.quartermaster` split into per-container helpers; `PrinterBlockEntity` pre-counts furnaces/chests + drains the shared budgets.
- [x] **Verdant Growth — identical Common/Uncommon descriptions** — DONE. The rarities genuinely DIFFER (`matureState`): **Common** ripens staple crops (wheat/carrot/potato/beetroot) + nether wart; **Uncommon** ALSO ripens cocoa & sweet berries. The shared "fully grown" blurb hid that — now each rarity shows its own line (`resin.verdant.common`/`.uncommon`, picked by tier in `ResinItem`). Both tiers kept (the breadth difference justifies them); say the word if you'd rather collapse to one.

**In-game verify (after the eventual redeploy):** spool reels clear instantly on GUI removal; resin tooltips/names/guide read Common/Uncommon/Rare + "consumable"; clock GUI has no footer; kelp farm clock fires + first kelp layer plants + front chest collects from furnaces (furnaces face out); Overdrive disc tooltip shows the struck-through preview; Quartermaster furnaces/chests come well-stocked; Verdant tooltips differ by rarity. **Jar NOT redeployed — Patrick still testing.**

---

## Retest round 10 (2026-06-18) — pumpkin/melon farm via player SCAN + Verdant stem fix — **NO jar redeploy**
- [x] **pumpkin_melon_farm — replaced the procedural build with PGMac's hand-built SCAN** — DONE. The generated farm never satisfied; Patrick rebuilt it in-game (working automated harvester) and scanned it. New flow: the raw scan is committed as a generator input at `src/test/resources/scanned/pumpkin_melon_farm.blueprint`, and `CuratedBlueprintGenerator.pumpkinMelonFarm()` now **loads + normalizes** it instead of building procedurally (new `loadScannedBlueprint` + `normalizeScannedFarm` + `youngStem` helpers). Two normalizations only, everything else **byte-for-byte**: (1) pumpkin/melon stems planted **age 0** (ungrown by default — Verdant matures them); (2) the chest's **test items stripped** (`tag.remove("Items")` on every BE). Verified preserved: the **staggered repeater delays (1/3/4** — Patrick flagged these are intentional), redstone routing, `sticky_piston[up]` harvesters, hoppers→chest (4,0,9), and the **redstone-clock baked interval** (`IntervalSeconds`). 9×5×10, 278 blocks; passes printability + all always-on audits with no allowlisting.
- [x] **Verdant "did nothing" on pumpkin/melon — root cause + fix** — DONE. `ResinEffects.matureState` handled `CropBlock`/`NetherWartBlock`/(T2) berries+cocoa but **not `StemBlock`**, so Verdant silently no-op'd on pumpkin/melon stems (Patrick's reported bug). Added `StemBlock → age 7` in the Common+ group (stems are staple crops). I had NOT fixed this earlier — round 9 only touched Verdant's descriptions, not the mechanic.

**In-game verify (after redeploy):** print the pumpkin/melon farm — stems come out young (ungrown), clock+pistons cycle, harvest routes to the chest; print it WITH a Verdant resin — stems come out mature (age 7). **Jar NOT redeployed — Patrick still testing.**

- [x] **chicken_coop_auto — SCRAPPED + archived (Patrick: can't get it to work)** — DONE. The lava-blade auto-cooker never cooked reliably (slow-falling chickens died in the lava; the earlier campfire version killed but left raw drops). Pulled it from the game entirely and archived for reference: moved the `.blueprint` + the generator method source + a revival README to `archive/chicken_coop_auto/` (not compiled, not loaded — doesn't render in game). Removed from the generator (`builds.put` + method), `CuratedBlueprints.CURATED_NAMES`, the `ModCreativeTabs` farm set, the `FarmCollectionGameTests` (`FARMS` entry + `@GameTest`), and the reachability + fire-hazard audit allowlists. Bundled blueprints 132 → 131; gametests 84 → 83; build green.

## Retest round 11 (2026-06-18) — glass villa + treasure loot — **NO jar redeploy**
- [x] **modern_glass_villa — remove the front water** — DONE. Tore out the sunken reflecting pool (water + prismarine basin + sea-lantern uplights + brick lining + coping); the front strip (z=0..1) is now a flat light-grey concrete entrance terrace. (Also dropped the now-unused `poolFloor`/`poolLining` palette vars.)
- [x] **modern_glass_villa — make the roof (3rd story) reachable** — DONE. Extended the existing interior ladder (NE column x=10, z=5) from the upper floor (y=6) all the way up to the **roof deck (y=10)**, hatching the deck so you climb out onto the roof terrace. Plugged the one `y=8` window-band cell behind the ladder with solid concrete so no rung pops (the unbacked-ladder class) — verified continuous + passes the ladder-support gate.
- [x] **Treasure resin — ~50% more loot (Patrick: loot-3 print felt light)** — DONE. Bumped the per-pool **rolls** ~50% across all three treasure tables: common 1-3 → 2-4, rare 2-4 → **3-6**, epic 2-4 → **3-6** (Rare resin pulls from rare + epic). Item stack sizes unchanged, so ~50% more item stacks per loot-bearing chest. *(Note: a single-chest build at the 0.5 per-chest T3 chance still whiffs ~half the time — if it feels sparse on small builds, bump `resin.treasureChanceT3` and/or `treasureCapT3` in config; say the word.)*

**In-game verify (after redeploy):** villa front has no water + a ladder climbs to the roof terrace; a Rare Treasure print drops noticeably more. **Jar NOT redeployed — Patrick still testing.**

---

## Round 12 (2026-06-18) — unify treasure loot pools to common/uncommon/rare + REDEPLOY
Earlier I'd left the treasure **loot pools** named common/rare/epic as a separate axis; Patrick wants the whole scheme unified with the resin rarity names. Mapping: `rare`→**uncommon**, `epic`→**rare** (common unchanged), so the pools now match the resin rarities (and progress common < uncommon < rare = iron/gold → diamond/emerald → netherite).
- [x] Renamed loot tables `treasure_rare.json`→`treasure_uncommon.json`, `treasure_epic.json`→`treasure_rare.json` (git mv; content preserved).
- [x] `ResinEffects.treasureTable` now returns common/uncommon/rare (Uncommon resin: common→uncommon; Rare resin: uncommon→rare).
- [x] Config renamed: `RESIN_TREASURE_T2_RARE`→`_T2_UNCOMMON` (key `treasureT2UncommonChance`), `RESIN_TREASURE_T3_EPIC`→`_T3_RARE` (key `treasureT3RareChance`) + comments; call site in `PrinterBlockEntity` updated.
- [x] `CatalystGameTests.treasureTableChoice` assertions updated; docs (catalysts-design Q8, MC3DPrint.md/.html, CHANGELOG) updated.

## Round 13 (2026-06-19) — redstone-repeater direction fix (all piston farms) + bamboo planting + wheat ungrown + REDEPLOY
- [x] **Repeaters were backwards on ALL piston farms (sugarcane/bamboo/kelp) → pistons never fired.** Confirmed the convention from the working pumpkin/melon SCAN (both its up-piston and horizontal-piston repeaters): **a repeater's `facing` points toward its INPUT/bus and it outputs the OPPOSITE way** — so to drive a piston the repeater must FACE THE BUS, not the piston. The generated farms had west=`facing=east`/east=`facing=west` (output pointing away from the pistons). Flipped to west=`facing=west`/east=`facing=east` (one `replace_all` — the 3 farms shared byte-identical lines) + a permanent convention comment so it can't regress.
- [x] **superSmelter** decorative readout repeater was `facing=north` (perpendicular to its E–W dust line, so it didn't connect) → `facing=east` (faces its comparator input). redstone_workshop's repeater is a static parts-showcase (no piston) — left as-is.
- [x] **Bamboo wasn't planting** (kept being skipped). Same fix as cactus/kelp: VALUED `minecraft:bamboo=2@1` + winder-blacklisted, and the bamboo farm now auto-plants the first shoot (`bamboo[age=0]` on each mud row, y=2). Plant-support audit already allows bamboo on mud. **Deleted stale config (FU change).**
- [x] **Wheat Farm grew everything fully (Verdant useless)** → all crops now `age=0` (wheat/carrots/potatoes), local to the wheat farm (the shared WHEAT/CARROTS/POTATOES age-7 constants stay for decorative builds). Verdant now matures them.

**Status: DONE + REDEPLOYED.** 83 gametests + all gates green. Rebuilt + redeployed the jar (single copy, checksums match) and deleted the game-instance config (bamboo FU). **Restart MC** to load it. **In-game verify:** sugarcane/bamboo/kelp pistons fire + harvest; bamboo prints planted; wheat farm prints ungrown (Verdant grows it).

## Round 14 (2026-06-19) — guide-book polish + T7 upgrade slots (NOT yet redeployed)
- [x] **Resin guide page title overflowed** ("Treasure vs Quartermaster" ran off the page) → split into two clean pages, "Treasure" and "Quartermaster".
- [x] **T7 printers now get 8 upgrade slots** (`MachineTier.upgradeSlots()` → `number >= 7 ? 8 : number`), so the top *visible* tier gets the full upgrade column without needing Draconic. T8 (hidden Draconic tier) is also 8. The GUI already supported 8 (2×4 grid, `MAX_UPGRADE_SLOTS=8`), so no GUI change. Guide text updated: "slots equal to its tier (T3 = 3, **T8 = 8**)" → "slots that grow with its tier (T3 = 3, **T7 = 8**)" in faq/upgrades + machines/upgrades — **T8 is never mentioned** (stays a Draconic surprise).
- [x] **Acknowledgements page** filled in (was a placeholder): thanks to TMTOWTDI (testing/rubber-duck) + Tristan (inspiration + signature builds). Split across two pages so it doesn't overflow.

Verified: 83 gametests + all gates green. **Not redeployed** — Patrick still reviewing the guide; build on request.

### Reusable: importing a hand-built farm from a scan
Patrick scans a build in-game → the native `.blueprint` lands in `…/saves/<world>/mc3dprint/blueprints/<uuid>.blueprint` (random UUID = player scan). I read the newest one directly from the Prism save, drop it at `src/test/resources/scanned/<name>.blueprint`, and point `<name>Farm()` at `loadScannedBlueprint` + `normalizeScannedFarm`. Same format as the curated bundle, so the mechanism (redstone/repeater stagger/BE NBT) survives intact.
